{
  "name": "@forge/prisma",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema and migrations for Claude Context",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "studio": "prisma studio",
    "format": "prisma format"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.3",
    "prisma": "^5.8.0",
    "tsx": "^4.7.0"
  }
}
